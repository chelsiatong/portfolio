import './contacts.scss';


function Contacts() {
    return (
        <section className="contacts" >
            <h1>contacts</h1>
            <p>add contact section here</p>
        </section>

    );
}

export default Contacts;